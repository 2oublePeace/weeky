url: http://localhost:8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/weeky
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
server:
  port: 8081
